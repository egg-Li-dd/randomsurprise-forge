# 文档分布 - randomsurprise-forge-new 项目

> 本文件记录项目中关键文件的功能位置，供后续任务快速定位。

---

## 兼容性 (compat)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/compat/CompatRegistry.java` | 兼容性注册器。在主模组构造阶段检测第三方模组（Jade/ToroHealth/Infernal Mobs）并注册对应集成。所有检测包裹在try-catch中确保任一兼容失败不影响主模组加载 |
| `src/main/java/com/randomsurprise/compat/InfernalMobsCompat.java` | AtomicStryker's Infernal Mobs 动态精英怪生成率调节。通过反射获取InfernalMobsCore.instance().getConfig()，修改eliteRarity/ultraRarity/infernoRarity值。每5秒(100tick)更新一次，根据4个因子动态计算：①玩家数量(每多1人降低5%稀有度值,最低0.7x) ②征召战场(战场期间0.5x基础+随难度递减,最低0.3x) ③坏词条数量(每个坏词条降低2%,最低0.7x) ④游戏进度(每完成1次征召降低1%,最低0.8x)。服务器关闭时恢复原始配置值。rarity值含义：1-in-N概率，值越低=生成率越高 |
| `src/main/java/com/randomsurprise/compat/jade/JadePlugin.java` | Jade悬浮提示集成，由Jade自动发现 |
| `src/main/java/com/randomsurprise/compat/torohealth/ToroHealthCompat.java` | ToroHealth HUD覆盖层集成 |

## 网络通信 (network)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/network/TimerSyncPayload.java` | 服务器->客户端 计时器同步数据包。编码方式：VarInt（remainingSeconds） |
| `src/main/java/com/randomsurprise/network/SuperPowerSyncPayload.java` | 服务器->客户端 超能力数据同步数据包。编码方式：UUID + UTF + Boolean + VarInt(cooldownSeconds) + VarInt(stateTicks) + VarInt(stateFlags.size) + UTF[] |
| `src/main/java/com/randomsurprise/network/ModNetworking.java` | 网络通道注册与发送方法入口。注册C2S包(ShopPurchasePayload/ExchangePayload/MoneyBagPayload/LotteryRequestPayload/LotteryConfirmPayload/ModBagActionPayload等)和S2C包(LotteryResultPayload/LotteryTenResultPayload/AffixSyncPayload/ExchangeListSyncPayload等)。sendExchangeList(ServerPlayer)发送稀有回收列表到客户端。sendToServer()发送C2S包。sendLotteryResult/sendLotteryTenResult发送抽奖结果(词条尚未应用，等待客户端回传LotteryConfirmPayload) |
| `src/main/java/com/randomsurprise/network/LotteryConfirmPayload.java` | C2S抽奖确认包（词条延迟生效机制核心）。字段：tenFold(boolean)+affixIds(List<String>)。handle()调用LotteryTicketItem.serverApply()——服务端收到后才真正应用词条。由LotteryScreen(单抽,动画结束/跳过/关闭时)与LotteryTenScreen(十连,进场动画结束/关闭时)发送，confirmSent幂等守卫防重复 |
| `src/main/java/com/randomsurprise/network/AffixSyncPayload.java` | 服务器->客户端 词条数据同步数据包 |
| `src/main/java/com/randomsurprise/network/ReviveActionPayload.java` | 客户端->服务端 复活动作数据包。自救分支已移除（targetPlayerId==自己时直接return），仅保留队友救助分支（setAllyReviving，服务端已改为靠近即可，右键为辅助） |
| `src/main/java/com/randomsurprise/network/ExchangeListSyncPayload.java` | 服务器->客户端 稀有回收物品列表同步数据包。内含EntryData内部类（entryId/itemIdString/cost/soldAmount）。handle()调用ExchangeRegistry.updateClientCache()更新客户端缓存，解决"稀有回收仅房主可见"问题 |
| `src/main/java/com/randomsurprise/network/ModBagActionPayload.java` | 客户端->服务端 模组袋快捷使用动作包。字段：actionType(0=净化药水,1=清除药水,2=抽奖券)。handle()先关闭玩家当前容器(closeContainer确保NBT一致)，找到手中模组袋，用"副手暂存"模式调用原始物品use()/serverRoll()逻辑：findBag检查主手/副手，freeHand返回无袋子的手，useTempItem暂存手中物品→放入目标物品→执行动作→恢复原手中物品(try/finally)。handlePurify/handleClear从袋子SimpleContainer找到对应物品removeItem后useTempItem调用item.use()。handleLottery同理调用LotteryTicketItem.serverRoll(player,false) |

## 平衡性配置 (config)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/config/BalanceConfig.java` | 平衡性配置中心。集中管理所有经济汇率（资源兑换/商店售价/回收白名单价）、属性封顶值（生命+30/吸血50%/回血5HP/s/移速60%/攻击+15）、坏词条全局封顶（血量2.0x/伤害1.5x/速度1.3x）、Boss HP玩家数缩放参数、超能力冷却时间、单人倒地自救窗口 |
| `src/main/java/com/randomsurprise/config/BalanceMath.java` | 平衡性计算工具（纯Java，无Minecraft依赖，可单元测试）。包含: hasArbitrage()资源兑换套利检测、hasShopRecycleArbitrage()商店回收套利检测、calculateCappedHostileHealth/Damage()坏词条加法叠加+封顶、capHealthBonus/Lifesteal/HpPerSecond/MoveSpeedPercent/BaseAttackDamage()属性封顶、calculateBossHpMultiplier() Boss总HP倍率(难度*玩家数) |
| `src/test/java/com/randomsurprise/config/EconomyBalanceTest.java` | 经济平衡性单元测试。验证绿宝石/铁锭/金锭/钻石兑换无套利、下界之星/龙蛋/鞘翅商店售价>=回收价 |
| `src/test/java/com/randomsurprise/config/AffixStackingTest.java` | 词条叠加与属性封顶单元测试。验证坏词条加法叠加(非乘法)、10人叠加封顶2.0x、生命/吸血/回血/移速/攻击力封顶、Boss HP随玩家数缩放 |

## 核心管理器 (root package)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/SurpriseManager.java` | 玩家计时器管理（每秒20tick），每1秒脏检查同步剩余时间到客户端 HUD。onServerTick 内含随机事件触发门控：isNegativeOnly(入侵态)与 isBattlefieldActive(战场进行态 PREP/ACTIVE/FINAL_BATTLE)时停止随机事件；DOOMSDAY 系列状态照常触发 |
| `src/main/java/com/randomsurprise/RandomSurpriseMod.java` | Mod 主类，事件总线注册。构造器中注册ModItems + ModBagMenu(容器菜单类型) + ModNetworking。onLivingHurt 处理伤害免疫：玩家总伤害免疫、敌对生物伤害免疫（最多60%，战场Boss通过 isBattlefieldBoss 排除以避免无法击败）、火焰/闪电伤害减免。倒地玩家免疫伤害+倒地玩家攻击取消（攻击者为倒地玩家时取消事件）。GUARDIAN_SHIELD伤害减免调用（SuperPowerHandler.onLivingHurtReduceDamage）。onLivingDeath 调用 SuperPowerHandler.onLivingDeath（BATTLE_FURY触发） |
| `src/main/java/com/randomsurprise/SurpriseConfig.java` | 随机惊喜配置（开关、间隔等） |
| `src/main/java/com/randomsurprise/SurpriseActions.java` | 随机惊喜事件执行逻辑 |
| `src/main/java/com/randomsurprise/WorldDataPath.java` | 世界数据存储路径工具 |

## 建筑生成系统 (root package + building)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/BuildingGenerator.java` | 随机建筑生成器。v17新增: tryGenerateModStructure(优先mod建筑+原建筑保底)、placeModernNbt(现代.nbt palette格式支持)、applyProperty(方块状态属性应用)。关键方法：generateBuilding(入口,50%概率优先mod结构)、spawnBeacon(信标+清空光柱)、generateBattleTower(返回塔高)、placeSchematic(旧版schematic格式)、findSuitableY(地表查找) |
| `src/main/java/com/randomsurprise/building/ModStructureLoot.java` | 模组建筑战利品配置。5级稀有度池(COMMON基础资源/UNCOMMON模组消耗品/RARE模组装备/EPIC稀有物品/LEGENDARY传说)。涉及已安装模组物品: Alex's Mobs/Ender Zoology/Friends&Foes/Mowzie's Mobs/Twilight Forest/Blue Skies/Cataclysm。关键方法: placeLootChest(放置单个箱子)、placeLootChestsInArea(搜索已有箱子+放置新箱子+填充战利品)、generateLoot(按ChestTier概率分布抽取) |
| `extract_structures.py` | 结构模组.nbt文件提取脚本。从已安装的mod jar中提取结构.nbt文件到schematics/目录。用法: python extract_structures.py [mods_dir] [output_dir] |

## 超能力系统 (superpower)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/superpower/SuperPowerHandler.java` | 超能力服务端核心逻辑（G键主动/V键被动/tick处理/数据同步含脏检查）。抽取使用公平袋子系统（Fair Bag）：powerBag包含全部16个超能力（含3个征召世界专属），每次抽取不放回，剩余≤3时补充并洗牌。随机数使用ThreadLocalRandom。超能力：BLINK瞬移/WEALTHY家财万贯/FLY飞行/ENCHANT附魔/THUNDER_STRIKE雷击/DECOMPOSE分解/PARKOUR跑酷/WEAPON_MASTER武器大师/TOXIN_IMMUNITY毒素免疫/NEAR_DEATH_RECALL濒死回溯/TIME_DILATION时间膨胀/LIFE_DRAIN生命汲取/GRAVITY_PULL重力操控/RAPID_RESCUE急速救援(征召)/BATTLE_FURY战场狂怒(征召)/GUARDIAN_SHIELD守护之盾(征召)。新增方法：doTimeDilation(5格敌对生物缓慢II 10秒)/doGravityPull(10格敌对生物拉拽+坠落伤害)/onLivingDeath(BATTLE_FURY击杀触发力量II)/onLivingHurtReduceDamage(GUARDIAN_SHIELD队友8格内减免20%)/onEntityDamaged中LIFE_DRAIN攻击恢复2HP |
| `src/main/java/com/randomsurprise/superpower/SuperPower.java` | 超能力枚举定义。含Type枚举(ACTIVE/PASSIVE/PASSIVE_WITH_TOGGLE/PASSIVE_WITH_ACTIVE)。新增battlefieldOnly字段（征召世界专属超能力标记，RAPID_RESCUE/BATTLE_FURY/GUARDIAN_SHIELD设为true，其余默认false）。提供6参数和7参数两个构造函数 |
| `src/main/java/com/randomsurprise/superpower/PlayerSuperPowerManager.java` | 玩家超能力数据持久化管理（冷却/状态tick/位置记录） |

## 词条系统 (affix)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/affix/PlayerAffixManager.java` | 玩家词条数据管理（持久化/好词条效果刷新/坏词条全局增强/战斗事件处理）。注意：好词条不再全局独占，addAffix/addRandomGoodAffix 不再向 ASSIGNED_GOOD_AFFIXES 添加记录，正面词条抽取后不从词条库移除。v12新增：onLivingHeal处理怪物回血减少、onEntityDamaged中狂战士伤害加成。v19新增：①不可去敌对词条系统——DRAW_COUNTS追踪抽取次数，每50次抽取添加PENDING_LOCK_MILESTONES，下一个坏词条自动锁定到LOCKED_AFFIXES，removeRandomBadAffix/clearAllBadAffixes跳过锁定词条；②离线玩家词条过滤——currentServer引用+6个全局计算方法(calculateGlobalHostileBoost/getTotalHostileFireResistance等)过滤离线玩家(getPlayerList().getPlayer(uuid)==null)，rebuildAssignedGoodAffixes不过滤(好词条保持分配)。持久化文件：draw_counts.json/locked_affixes.json |
| `src/main/java/com/randomsurprise/affix/Affix.java` | 词条数据结构定义。v12新增4个字段：healReductionPercent（坏词条抑制回血%）、antiHealOnHitPercent（好词条攻击附带抑制回血%）、berserkerThreshold（狂战士触发血量阈值）、berserkerDamageBonus（狂战士攻击力加成倍率）。含v12兼容构造器（55个效果参数，fireBurnStackBonus/blindnessResistancePercent/moveSpeedPercent/baseAttackDamage/hpPerSecond默认为0） |
| `src/main/java/com/randomsurprise/affix/AffixRegistry.java` | 词条注册表。rollAffix() 抽取逻辑：先决定好坏(70%/30%)再 roll 稀有度(权重随全服坏词条数动态提升，上限0.15)，好词条使用 findAffix 均匀随机选择（不再全局独占过滤）。findAffix 收集同稀有度所有词条后用 RANDOM.nextInt 均匀抽取。v12新增9个词条注册：6个坏词条抑制回血系列（bad_heal_reduction_common~mythic，healReductionPercent=15/25/35/50/70/90）+3个好词条（good_life_steal_legendary吸血8%/good_berserker_mythic血量<50%攻击+50%/good_anti_heal_epic攻击附带3秒50%抑制回血） |
| `src/main/java/com/randomsurprise/affix/AffixRarity.java` | 词条稀有度枚举 |
| `src/main/java/com/randomsurprise/affix/MoneyBagItem.java` | 钱袋子物品。NBT存储金币（key: "coin_value"）。关键方法：getCoinValue/setCoinValue/addCoins/removeCoins（静态，传ItemStack）。右键打开UI，Shift+右键快速存入背包金币。不可堆叠 |
| `src/main/java/com/randomsurprise/affix/ModBagItem.java` | 模组袋物品（EPIC稀有度，不可堆叠）。NBT存储9格物品内容物（key: "BagItems"）。关键方法：loadContainer(读NBT无监听器)/getContainer(读NBT+加监听器自动保存)/saveContainer(写NBT)。use()右键通过MenuProvider打开ModBagMenu容器界面。isFoil返回true(附魔光效)，appendHoverText显示提示。禁止套娃（Slot.mayPlace阻止放入ModBagItem） |
| `src/main/java/com/randomsurprise/affix/LotteryTicketItem.java` | 抽奖券物品。**词条延迟生效两步机制**：①serverRoll(ServerPlayer, tenFold)只抽奖不应用——检查入侵态禁用/手持券/数量→消耗券→rollAffix→暂存结果到PENDING_ROLLS(Map<UUID,List<String>>)→发送结果包(LotteryResultPayload单抽/LotteryTenResultPayload十连)给客户端，词条尚未生效；②serverApply(ServerPlayer, tenFold, affixIds)由客户端LotteryConfirmPayload回调触发——优先用PENDING_ROLLS暂存结果(防作弊)→addAffix→applyGoodEffects→broadcastBadAffix→syncAllToClients。常量REQUIRED_EXP_LEVEL=10。broadcastBadAffix广播坏词条全服消息。use()右键打开LotteryStartScreen |
| `src/main/java/com/randomsurprise/affix/HostileEnhancer.java` | 敌对生物增强器。enhanceHostile(Mob) 在实体生成时调用，根据全局坏词条增强血量/攻击/速度/火焰免疫/生命恢复。方法开头排除战场Boss（调用 BattlefieldManager.isBattlefieldBoss），避免与战场 enhanceBoss 双重增强导致属性叠加过高 |

## 征召战场系统 (battlefield)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/battlefield/BattlefieldManager.java` | 征召战场状态机管理。核心常量：EVENTS_PER_BATTLEFIELD=30、PREP_ROOM_DURATION=600(30s)、WAVE_INTERVAL_TICKS=600(30s波次固定间隔)、BASE_WAVES=3。波次系统：spawnWave末尾设置nextWaveCountdown=WAVE_INTERVAL_TICKS；tickBattlefield中倒计时到0时若有剩余波次则currentWave++并spawnWave；onBossDeath仅从aliveBosses移除，不再推进波次。胜利条件=currentWave>=totalWaves&&aliveBosses空→调用onVictory()进入BATTLEFIELD_VICTORY停留阶段（v20）。v20改动：①clampBattlefieldMobs缓冲改为ARENA_RADIUS+1=36格，越界时传送到最近边缘点（归一化向量×ARENA_RADIUS）②onVictory:kill所有Enemy(保留掉落)+应用奖励+保存傀儡+进入30s停留+同步BattlefieldVictoryPayload到客户端 ③tickVictory:倒计时+每秒同步+到0传送所有剩余玩家+检查全员退出 ④requestEarlyExit:玩家长按空格5s后请求退出 ⑤teleportPlayerFromVictory:单玩家传送+傀儡跟随 ⑥checkAllPlayersExited:全员退出后调用resetBattlefieldAfterVictory ⑦resetBattlefieldAfterVictory:清除掉落物/残留实体+resetArenaMarker+状态转移(与原endBattlefield成功路径一致)+发送结算画面 |
| `src/main/java/com/randomsurprise/battlefield/BattlefieldDimension.java` | 征召维度注册与传送工具。常量：BATTLEFIELD_DIM_KEY(维度Key)、ARENA_CENTER_X/Y/Z=0/64/0、ARENA_RADIUS=35（70x70总尺寸）。方法：getBattlefieldLevel、teleportToBattlefield、isInBattlefield |
| `src/main/java/com/randomsurprise/battlefield/GameState.java` | 征召战场全局状态枚举。含 isBattlefieldActive(PREP/ACTIVE/FINAL/VICTORY)、isDoomsday、isInvasion、isTerminal 等判断方法。v20新增BATTLEFIELD_VICTORY状态（胜利停留30s，isBattlefieldActive返回true以阻止随机事件） |
| `src/main/java/com/randomsurprise/battlefield/BossPool.java` | Boss 池定义。排除集合：EXCLUDED_ENTITY_IDS(非Boss)、SPECIAL_DEFEAT_BOSSES(需特殊方式击败的Boss，如末影龙/远古守卫者/暮色森林Hydra等11个，从randomBoss与getFinalBattleBosses中过滤)。关键方法：randomBoss(int difficulty)(按难度选Boss并过滤特殊Boss，兜底ravager)、randomHostileMob()(随机小怪，无参)、getBossesForDifficulty、getFinalBattleBosses(决战池，过滤特殊Boss) |
| `src/main/java/com/randomsurprise/battlefield/DownedStateManager.java` | 倒地状态管理器。倒地玩家免疫伤害、不被怪物攻击（每tick清除仇恨）；自救已取消，只能由队友靠近5格内持续救助复活。倒地效果：缓慢IV+虚弱IV+致盲+发光+隐身。RAPID_RESCUE超能力将救助时间从100tick(5秒)降为40tick(2秒)。无队友时救助进度每tick衰减-2 |

## 商店系统 (shop)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/shop/ShopManager.java` | 商店服务端核心。静态块定义所有商品条目（ENTRIES），分类含金币兑换/模组物品/强力附魔书/稀有物品/武器装备/预附魔装备/消耗品/资源兑换/模组装备/稀有回收。关键方法：getCoinCount（含钱袋子金币统计）、getCurrencyCount、processPurchase（单次/批量购买验证+扣款+发放）、processExchange（稀有回收）、deductCurrency（货币扣除分发）、deductCoins（金币扣除：优先松散金币，不足时从钱袋子扣除差额）、addCoinsToInventory（绕过64堆叠限制）。购买记录持久化（JSON） |
| `src/main/java/com/randomsurprise/shop/ExchangeRegistry.java` | 稀有物品回收注册表。扫描 BuiltInRegistries.ITEM 按 Rarity 识别可回收物品。配置文件 config/randomsurprise_exchange.json（白名单/黑名单/自定义价格）。关键方法：init/reload（异常保护+保底物品）、scanRegistry（每物品try-catch）、addFallbackEntries（保底6个原版稀有物品）、getExchangeEntries（客户端侧返回clientCachedEntries缓存，服务端侧返回EXCHANGE_ENTRIES扫描结果）、updateClientCache（接收服务端同步数据更新客户端缓存）、getEntriesForSync（序列化数据供网络同步）。默认价格：epicPrice=12、rarePrice=6 |
| `src/main/java/com/randomsurprise/client/ShopScreen.java` | 商店客户端界面（Screen）。多标签页+商品列表+购买/兑换弹窗+数量选择弹窗。关键方法：countCurrency（客户端货币统计，UNIVERSAL_COIN分支含钱袋子金币）、countItem、renderResourcePanel（右上角金币/券面板）、isAffordable、checkQtyResources、confirmQtyAction |
| `src/main/java/com/randomsurprise/client/ModBagScreen.java` | 模组袋容器界面（AbstractContainerScreen<ModBagMenu>）。imageWidth=176/imageHeight=180。4个快捷使用按钮：使用净化药水/使用清除药水(y=128)/使用抽奖券/关闭(y=150)，2行2列布局。containerTick()每tick根据袋子内容物动态启用/禁用按钮(hasItem检查menu.getBagContainer)。sendAction()发送ModBagActionPayload后onClose()关闭。renderBg绘制深色面板(0xF51A1A2E)+紫色边框(0xFFA020F0)+槽位凹陷。renderLabels绘制标题(5参drawString)。无render重写(基类已处理renderBackground/slots/widgets/labels/tooltip)。嵌套Registration类用@Mod.EventBusSubscriber(Dist.CLIENT,Bus.MOD)在FMLClientSetupEvent中MenuScreens.register绑定Screen |

## 容器菜单 (menu)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/menu/ModBagMenu.java` | 模组袋容器菜单（AbstractContainerMenu）。使用DeferredRegister注册MenuType到ForgeRegistries.MENU_TYPES，通过IForgeMenuType.create()创建（3参lambda: windowId/inv/data）。9格袋子槽位(y=20)+玩家背包3x9(y=44)+快捷栏9格(y=102)。Slot.mayPlace禁止放入ModBagItem防递归嵌套。quickMoveStack实现袋子↔玩家背包shift-click移动。两个构造器：客户端(int,Inventory)创建空容器，服务端(int,Inventory,SimpleContainer)使用袋子真实容器。register(IEventBus)在主模组构造时注册 |

## 客户端 (client)

| 文件路径 | 功能说明 |
|---------|---------|
| `src/main/java/com/randomsurprise/client/ClientRandomSurpriseMod.java` | 客户端入口。监听R/G/V键、跑酷二段跳、背包商店按钮、附魔书拖拽应用。handleReviveRightClick：自救已取消（自己倒地时右键无效），仅保留队友救助右键辅助逻辑 |
| `src/main/java/com/randomsurprise/client/TimerHud.java` | 客户端计时器 HUD 渲染 |
| `src/main/java/com/randomsurprise/client/ClientSuperPowerData.java` | 客户端超能力数据缓存 |
| `src/main/java/com/randomsurprise/client/OreSenseRenderer.java` | ORE_SENSE 渲染器（已禁用）。ORE_SENSE 超能力已移除，不再注册到事件总线，方法体已清空。保留文件以备将来参考 |
| `src/main/java/com/randomsurprise/client/LotteryStartScreen.java` | 抽奖准备界面（Screen）。主界面含单抽/十连抽/取消按钮 + 问号按钮(查看概率详情浮层)。概率浮层使用 scissor 裁切防溢出，两列布局(增益/敌对)按稀有度分组显示。calculateRarityProbabilities() 客户端概率计算(上限0.15，与服务端一致) |
| `src/main/java/com/randomsurprise/client/LotteryScreen.java` | 单抽转盘界面（Screen，厚重金属沉浸风格·增强版）。横向滚动老虎机式词条抽奖动画。常量：CELL_WIDTH=120/CELL_HEIGHT=72/VISIBLE_CELLS=9/ANIMATION_DURATION=200(10秒，更慢更有紧张感)。配色：深灰铁#2A2A2E/暗钢#3D3D42/亮暗钢#4A4A52/暗金#B8860B/亮暗金#D4A437/血锈红#8B2500/宝石红#B23020/银#999999/亮银#BCBCBC/深黑底#0A0A0C。构造器 public LotteryScreen(String affixId)，init()构建wheelAffixes(5次重复)+targetIndex(第3圈)+初始化18个环境粒子+重置震动/火花/爆发列表+confirmSent=false。动画：三次方缓出+末端微微前冲后沉稳停住；指针逐格抖动+铁砧落地咔哒音(ANVIL_LAND 0.15/0.6-0.8递减)+抖动生成2-3个金色火花粒子(progress>0.3)。**词条延迟生效机制**：动画结束(animationDone=true)时调用sendConfirm()发送LotteryConfirmPayload(false, [affixId])到服务端，词条此时才真正生效；sendConfirm()幂等(confirmSent守卫)。**空格加速**：tick()中GLFW.glfwGetKey检测空格按下，每tick额外+2进度(3x速)。**左击跳过**：mouseClicked(button==0)动画中调用skipAnimation()(立即animationDone=true+revealTicks=40稳定结果+sendConfirm)；动画结束后左击onClose()关闭。**onClose兜底**：sendConfirm()防券白损失。十大增强：①环境余烬粒子AmbientParticle(15-20个，金#D4A437/暗铁#4A4A52，从底向上漂浮+sin水平摆动，life100-200)；②暗角晕影renderVignette(上下80px/左右60px渐变黑alpha0→120)；③屏幕震动(停止时screenShake=8/intensity=3.0F，render开头随机偏移translate，tick衰减0.85)；④三层金属边框(外深灰铁6px+银高光/中暗金2px/内亮钢1px)+L形角落加固板renderCornerPlate(24px臂/3px厚/斜切角/银高光)+双排铆钉renderRivet(第二排偏移spacing/2)；⑤增强格子renderCell(深斜面4px三层渐变高光+阴影/2-3条1px划痕/2-3个2x2磨损斑点/格子间分隔线深灰铁+银高光/hue±5根据index微调shiftHue)；⑥增强指针renderPointer(h=20/楔形halfW=12/圆形枢轴r=5/下方投影/抖动火花SparkParticle)；⑦双层中心齿轮renderCenterGear(外8齿CW r=21+内6齿CCW×1.5速r=13/宝石脉冲发光sin(gearAngle×0.08)/圆形阴影fillCircleRing)；⑧速度线renderSpeedLines(progress<0.4叠加6条半透明银水平线)；⑨五阶段揭晓renderReveal(闪光TOTEM_USE/暗下/25个BurstParticle爆发带重力0.08/火焰金光sin±3px波浪+亮度脉动/稳定金边2px发光晕+烟雾粒子)；⑩增强音效(停止ANVIL_LAND0.5/0.4+WITHER_DEATH0.2/0.6/闪光TOTEM_USE0.4/0.8/金光升起3个AMETHYST_BLOCK_CHIME 0.8/1.0/1.2递增/共鸣NETHERITE_BLOCK_STEP每20tick)。粒子内部类：AmbientParticle/SparkParticle/BurstParticle(总粒子<60)。工具：mix(颜色混合)/withAlpha(int/float)/shiftHue(RGB→HSV→偏移→RGB)/fillCircle(步长2扫描线)/fillCircleRing。无ResourceLocation纯绘制。renderResult显示稀有度标签[mix暗金+rarityColor 0.4]/名称(getNameKey)/描述(getDescKey)/关闭提示(左击或ESC)。动画期间显示spinning_hint(空格加速\|左击跳过)。参照实现SuperPowerLotteryScreen |
| `src/main/java/com/randomsurprise/client/SuperPowerLotteryScreen.java` | 超能力转盘界面（Screen，厚重金属沉浸风格·增强版，与LotteryScreen共享同一套金属美学与十大增强）。横向滚动老虎机式超能力抽奖动画。常量：CELL_WIDTH=120/CELL_HEIGHT=80(比词条格子高8px，超能力名称更长)/VISIBLE_CELLS=9/ANIMATION_DURATION=120(6秒)。配色与LotteryScreen完全一致(深灰铁#2A2A2E/暗钢#3D3D42/亮暗钢#4A4A52/暗金#B8860B/亮暗金#D4A437/血锈红#8B2500/宝石红#B23020/银#999999/亮银#BCBCBC/深黑底#0A0A0C)。构造器 public SuperPowerLotteryScreen(String superPowerId)，标题superpower.randomsurprise.lottery_title；init()构建wheelPowers(Arrays.asList(SuperPower.values())重复5次)+targetIndex(第3圈)，superPower=SuperPower.getById(superPowerId)，初始化18个AmbientParticle+重置screenShake/spark/burst列表。关键差异(对比LotteryScreen)：无稀有度，格子色调mix(COL_STEEL_LIGHT, sp.getColor(), 0.30)用超能力颜色；格子内容=类型标识(◆主动/◆被动，sp.hasActiveAction()判定，主动COL_DARK_GOLD/被动COL_SILVER)+超能力名称(Component.translatable(getNameKey())按像素宽度截断)；揭晓renderResult显示类型标签+名称(getNameKey)+描述(getDescKey)+关闭提示(lottery.randomsurprise.close_hint)；滚动标题superpower.randomsurprise.lottery_spinning。十大增强(与LotteryScreen一致)：①环境余烬粒子AmbientParticle(18个，金#D4A437/暗铁#4A4A52，从底向上漂浮+sin水平摆动，life100-200)；②暗角晕影renderVignette(上下80px/左右60px渐变黑alpha0→120)；③屏幕震动(停止时screenShake=8/intensity=3.0F，render开头pushPose+translate随机偏移，tick递减)；④三层金属边框renderMetalFrame(外深灰铁6px+银高光/中暗金2px/内亮钢1px)+L形角落加固板renderCornerPlate(24px臂/3px厚/斜切角/银高光)+双排铆钉renderRivet(第二排偏移spacing/2)；⑤增强格子renderCell(深斜面4px三层渐变高光+阴影/2-3条1px划痕基于0x9E3779B1种子/2-3个2x2磨损斑点/格子间分隔线深灰铁+银高光/hue±5根据index微调shiftHue)；⑥增强指针renderPointer(h=20/楔形halfW=12/圆形枢轴r=5/下方投影/抖动火花SparkParticle)；⑦双层中心齿轮renderCenterGear(外8齿CW r=21+内6齿CCW×1.5速r=13/宝石脉冲发光sin(gearAngle×0.08)/圆形阴影fillCircleRing)；⑧速度线renderSpeedLines(progress<0.4叠加6条半透明银水平线)；⑨五阶段揭晓renderReveal(闪光TOTEM_USE/暗下/25个BurstParticle爆发带重力0.08/火焰金光sin±3px波浪+亮度脉动/稳定金边2px发光晕+烟雾粒子)；⑩增强音效(停止ANVIL_LAND0.5/0.4+WITHER_DEATH0.2/0.6/闪光TOTEM_USE0.4/0.8/金光升起3个AMETHYST_BLOCK_CHIME 0.8/1.0/1.2递增/共鸣NETHERITE_BLOCK_STEP每20tick)。粒子内部类：AmbientParticle/SparkParticle/BurstParticle(总粒子<60)。工具：mix(颜色混合)/withAlpha(int/float)/shiftHue(RGB→HSV→偏移→RGB)/fillCircle(步长2扫描线)/fillCircleRing。无ResourceLocation纯绘制。调用方：SuperPowerRollPayload。废土感(Wasteland)+MC风格增强(叠加于十大增强之上，不删除原有功能)：新增颜色常量COL_MOSS(#3D5C3D苔藓)/COL_RUST_1/2/3(#8B4513/#A0522D/#CD853F锈蚀)/COL_CRACK(#0A0A0A)/COL_REDSTONE(#FF0000)/COL_ENCHANT(#8B5CF6)/COL_SCORCH(#8B2500)/COL_DUST(#6B6B6B)+MC方块配色参考注释(IRON_BLOCK/DAMAGED_ANVIL/GOLD_BLOCK/REDSTONE_BLOCK/MOSSY_COBBLESTONE)；①renderRustPatches(确定性伪随机3-5个橙棕锈蚀斑点2x2~4x3px alpha60-100，renderMetalFrame边框+renderCell格子调用)；②renderCracks+drawCrackLine(Bresenham线段，1-2条锯齿裂纹3-5段±30°偏转5-15px alpha200，边框上下+部分角落调用)；③苔藓氧化(COL_MOSS 2px渐变绿，角落加固板内侧边缘+铆钉周围3x3 alpha20晕+齿轮齿间隙)；④灼烧痕迹(renderScorchMark 3x3黑色渐变指针尖端+renderRadialScorch 4条短线宝石周围+renderScorchBorder暗红#8B2500渐变揭晓金光边缘)；⑤灰尘粒子(AmbientParticle增加dust类型，灰#6B6B6B/水平漂移vx0.2~0.5/无重力/alpha30-50/与余烬比例2:1，spawnSmokeParticle强制余烬)；⑥破损铆钉(renderRivet约15%基于x伪随机只画暗色底+裂纹像素+脱落碎片2x1)；⑦像素艺术铆钉(主体2x2+高光1x1左上+暗部1x1右下)；⑧红石粉装饰(renderRedstoneTraces 2-3个1x1红石粉点alpha80+极淡连接线alpha20)；⑨附魔光效(revealTicks>=34周期性闪烁紫色#8B5CF6 alpha0-60 sin(tick×0.15)每隔2px水平线)；⑩MC耐久度条(renderResult底部boxW-8宽2px高/背景黑/前景暗金70%填充/末端红#FF4444 1px磨损)；⑪MC风格文字阴影(标题drawString 1px偏移暗影层+§l粗体类型标签增强雕刻感) |
| `src/main/java/com/randomsurprise/client/LotteryTenScreen.java` | 十连抽结果展示界面（Screen）。2列x5行网格布局展示10个抽中词条。构造器 public LotteryTenScreen(List<String> affixIds)，存affixIdsRaw用于确认包。进场动画ANIM_DURATION=20+CELL_DELAY=2逐格淡入(alpha缩放)。**词条延迟生效**：进场动画结束(tick达到ANIM_DURATION+COLS*ROWS*CELL_DELAY)时sendConfirm()发送LotteryConfirmPayload(true, affixIdsRaw)，词条此时才生效；onClose()兜底sendConfirm()防券白损失；confirmSent幂等。mouseClicked左击关闭(经onClose发确认)。每个格子显示稀有度色边框+名称(getNameKey)+稀有度标签。调用方：LotteryTenResultPayload |

## Skill 文件 (trae/skills)

| 文件路径 | 功能说明 |
|---------|---------|
| `.trae/skills/randomsurprise-data/SKILL.md` | **数据手册 skill**。包含完整词条列表(108个)、随机事件列表(96个)、超能力列表(16个)、征召战场配置、商店配置、修改指南。用户可直接编辑此文件来指定需要修改的内容 |
| `C:\creategame\MCzhenghebao\.trae\skills\randomsurprise-blueprint\SKILL.md` | **蓝图 skill**。完整功能蓝图与架构说明，含当前状态概览、系统详解、网络通信、文件结构、版本历史。修改模组功能/玩法后必须同步更新 |
| `C:\creategame\MCzhenghebao\.trae\skills\mod-deployment\SKILL.md` | **模组部署 skill**。管理 Forge 1.20.1 项目的构建与部署，含 Gradle 构建命令、jar 部署位置、当前部署模组列表 |
